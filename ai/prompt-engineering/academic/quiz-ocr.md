The uploaded PDF is a midterm from an ~~undergraduate operating systems~~ class.

Produce 3 files:
1. ungraded file: markdown with questions only, likely generated by image-based OCR
2. graded file: markdown file with the question, the answer, and how it was graded with any correction notes., likely generated by image-based OCR
3. a zip file of the session: should contain all contents in `/mnt`, `/tmp` and `/content` if those folders exist. As a fallback, after .zip creation, output the base64 encode of the created .zip file in the chat output so that its contents can be deserialized later.

Multiple choice questions should be formatted according to the `## Multiple Choice Template` heading. The ungraded file should not mark the correct answer while the graded file should.

Fill in the blank questions should be formatted according to the `## Fill-in-the-Blank Template` heading. The ungraded file contains only escaped underscores while the graded file has the correct answer underlined.

## Multiple Choice Template
#### M{question-number-if-exists}: Question Topic
{question-text}
    - a) `[ ]` {a-response-text};
    - b) `[x]` {b-response-text, marked if correct};
    - c) `[ ]` {c-response-text};
    - d) `[ ]` {d-response-text};

## Fill-in-the-Blank Template
#### B{question-number-if-exists}: Question Topic
Question text with `______` used for the blank in the ungraded file and <u>underlined</u> in the graded file
