<!--Simplistic-->


This PDF applies ~~Principle Component Analysis (PCA) to a small dataset~~ for the purposes of an academic assignment.

Analyze the PDF for mistakes in theory, methodology, or conclusion. List these areas for improvement.

<hr>


<!--Better-->


The attached PDF is a submission for an ~~undergraduate upper division probability and statistics~~ class.

For each question posed:
1. Is the provided answer correct? Respond with "correct" or "incorrect" first, followed by a discussion why it is incorrect?
2. Can the provided answer be improved with better conciseness, more clarity, or illustrative formatting?


<hr>


<!--Advanced-->

The attached Markdown is a summary of topics discussed in an ~~undergraduate Probability and Statistics~~ class.

Create 4 files:
1. `scorecard.md`: a scorecard written in markdown with columns: "topic", "summary", "correct/incorrect".
2. `analysis.md`: a long-form analysis. for each topic posed, this file contains:
    1. the topic as a header
    2. "Correctness" as a subheader with an answer to "Is the provided answer or analysis correct"? Respond with "correct" or "incorrect" first, followed by a discussion why it is incorrect, followed by a final recommended answer.
    3. "Conciceness" as a subheader with an answer to "Can the provided answer or analysis be improved with better conciseness, more clarity, or illustrative formatting"? Respond with "consice" or "ambiguous" first, followed by a discussion, followed by a final recommended answer.
3. `answer.md`: a straightforward collation of the answers. for each topic posed, this file only contains the answer to the topic.
4. `session.zip`: an archive of steps taken, output as a hosted .zip file and a base64 encoded string, further details in the `## Important` header.

## Important:
- If diagrams are generated, archive all of the generated images to a .zip file and keep the references in the markdown relative to the same directory.
- create a separate backup .zip file that archives the contents of `/mnt` and `/tmp` within reason and hosts it as a downloadable file.
    - as a fallback, the base64 encoded string of the .zip file should be posted in the chat as a secondary deserialization source in case something goes wrong.


<hr>
